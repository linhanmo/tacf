# Experiment configuration stubs.

See ``default.yaml`` in the project-level ``configs/`` folder at the repository root for
the main training hyperparameters.  This folder is reserved for experiment-local
overrides used by the ablation/robustness/synthetic runners.
